# Chart visual report

- Full suite: **462/462 PASS** (threshold 0)
- Branched from origin/main after Carousel #61 (`b84caa8`)
- Main baseline after Carousel (#61): 456/456
- Chart cases: **6/6**, **0px** vs official shadcn/ui

Official `chart.json` is still the **Recharts v3** wrapper (`ChartContainer` / `ChartTooltip` / `ChartLegend`). Not Base UI. StyleX uses the same primitive.

## Chart per-case pixels

| Case | Result | Mismatched pixels |
| --- | --- | ---: |
| `chart__light__bar` | PASS | 0/109824 |
| `chart__light__line` | PASS | 0/109824 |
| `chart__light__tooltip` | PASS | 0/109824 |
| `chart__dark__bar` | PASS | 0/109824 |
| `chart__dark__line` | PASS | 0/109824 |
| `chart__dark__tooltip` | PASS | 0/109824 |

Skipped: the rest of the official charts gallery (area, pie, radar, radial, interactive), Card chrome around the primitive, and legend-only variants. Captures pin Jan–Jun 2024 data and `isAnimationActive={false}` on Bar / Line / Tooltip. `bar` matches official chart-bar-default (no hover). `line` matches official chart-line-default. `tooltip` is the bar chart with `defaultIndex={1}` (February). Viewport 480×400 (below Tailwind `md`). Combobox harness `caretColor: transparent` is unchanged. Calendar remains pinned to 15 June 2024. Carousel remains `watchDrag: false` / `duration: 0`.

Did not add Sidebar, Sonner, Form, or Carousel docs. Did not merge.

Generated by `npm run visual:diff`. Full tables: `visual/results/report.md`.
