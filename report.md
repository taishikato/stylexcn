# Form visual report

- Full suite: **480/480 PASS** (threshold 0)
- Branched from origin/main after Sonner #67 (`ee8d7e8`)
- Conflicts: none
- Main baseline after Sonner #67: 474/474
- Form cases: **6/6**, **0px** vs official shadcn/ui

Official `form.json` is still the **react-hook-form** wrapper (`FormProvider` / `Controller` / FormItem / FormLabel / FormControl / FormDescription / FormMessage). It does not compose Field. Not Base UI. StyleX uses the same primitive.

## Form per-case pixels

| Case | Result | Mismatched pixels |
| --- | --- | ---: |
| `form__light__default` | PASS | 0/51264 |
| `form__light__error` | PASS | 0/65088 |
| `form__light__message` | PASS | 0/59328 |
| `form__dark__default` | PASS | 0/51264 |
| `form__dark__error` | PASS | 0/65088 |
| `form__dark__message` | PASS | 0/59328 |

Skipped: zod / `@hookform/resolvers`, checkbox/select/radio FormField demos, and the rest of the official form gallery. Captures pin `defaultValues.username` to `shadcn`. `error` uses `setError` with `shouldFocus: false`. `message` shows FormMessage children with no error. Viewport 480×400 (below Tailwind `md`). Combobox harness `caretColor: transparent` is unchanged. Calendar remains pinned to 15 June 2024. Carousel remains `watchDrag: false` / `duration: 0`. Chart data stays pinned with animations off. Sidebar open state stays pinned. Sonner remains `duration={Infinity}`.

Did not add Toast (deprecated) or Sonner docs. Did not merge.

Generated by `npm run visual:diff`. Full tables: `visual/results/report.md`.
